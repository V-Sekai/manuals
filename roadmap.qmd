---
title: "Roadmap"
format: 
  html:
    page-layout: full
---

```{python}
#| label: roadmap
#| echo: false

import pandas as pd
from itables import init_notebook_mode, show

init_notebook_mode(all_interactive=False)

df = pd.read_csv("roadmap/roadmap.tsv", sep="\t")

show(df, paging=False)
```
