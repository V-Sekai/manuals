[project]
name = "manuals"
version = "0.1.0"
description = "Project for roadmap"
authors = [ { name = "K. S. Ernest (iFire) Lee" } ]
requires-python = ">=3.12"
dependencies = [
    "scipy",
    "pandas",
    "plotly",
    "itables",
    "quarto-cli",
    "pymoo",
]
